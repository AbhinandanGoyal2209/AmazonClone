DATABASE_URL="postgresql://postgres:postgres@localhost:5432/amazon_clone?schema=public"
PORT=4000
CORS_ORIGIN="http://localhost:5173,http://localhost:5174"
JWT_SECRET="dev_secret_change_me"

# Email (optional). If not set, app uses Ethereal test account and logs preview URL.
# Note: For Gmail, use an App Password, not your regular password.
SMTP_HOST="smtp.gmail.com"
SMTP_PORT=465
SMTP_USER="abhi2005goyal@gmail.com"
SMTP_PASS="sncs qyyo raqs ggax"
MAIL_FROM="Amazon Clone <no-reply@amazon-clone.dev>"

# Stripe (optional for now)
STRIPE_SECRET_KEY=""
STRIPE_SUCCESS_URL="http://localhost:5174/checkout/success?session_id={CHECKOUT_SESSION_ID}"
STRIPE_CANCEL_URL="http://localhost:5174/checkout"

